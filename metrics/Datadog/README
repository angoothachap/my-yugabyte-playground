Replace <<YOUR_DATADOG_KEY>> with your Datadog API key

Run `docker-compose up -d`

Check Datadog for metrics. You should be able to see them under namespace `yugabyte_prom` as shown [here](./static/DD_metrics.jpg) 